import 'package:flutter/material.dart';

class Game2DetailPage extends StatelessWidget {
  const Game2DetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Oyun 2 Detay')),
      body: const Center(child: Text('Burası Oyun 2 Detay Sayfası')),
    );
  }
}
