import 'package:flutter/material.dart';

class Game3DetailPage extends StatelessWidget {
  const Game3DetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Oyun 3 Detay')),
      body: const Center(child: Text('Burası Oyun 3 Detay Sayfası')),
    );
  }
}
